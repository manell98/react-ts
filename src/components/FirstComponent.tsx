const FirstComponent = () => {
    const nomeComponent = "Primeiro Component no React";

    return (
        <div>
            <h1>{nomeComponent}</h1>
        </div>
    )
}

export default FirstComponent;